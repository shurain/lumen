{
  "name": "lumen",
  "version": "0.1.0",
  "description": "A beautiful, live-reloading Markdown previewer for macOS — a modern Marked.app clone built with web tech.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json"
  }
}